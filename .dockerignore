# Dependencies (reinstalled in the image)
node_modules
npm-debug.log

# Build output (regenerated in the image)
dist

# Secrets & env (injected at runtime, never baked in)
.env
.env.*
!.env.example

# Version control
.git
.gitignore

# Tests & coverage
coverage
**/*.spec.ts
test

# Editor / OS
.vscode
.idea
.DS_Store

# Logs
logs
*.log

# Docker files themselves
Dockerfile
docker-compose*.yml
.dockerignore